# 2026-09-12

## Checked the 32 x.com feeds — all 503, config problem not URL problem
- RSSHub is up (GET / -> 200) but `/twitter/user/<handle>` throws
  `ConfigNotFoundError: Twitter API is not configured` for all 32 feeds.
- Local RSSHub checkout: `/Users/ivanwong/Code/business/rsshub` (dev mode, `registry-dev.ts`).
  Config comes from `lib/config.ts` via `dotenv/config` -> a `.env` in that repo root.
  No `.env` exists yet (only `.envrc`, which is a nix/direnv shim).
- Required env, from `lib/routes/twitter/api/index.ts`:
  - `TWITTER_AUTH_TOKEN` (comma-separated; web API path) **or** `TWITTER_THIRD_PARTY_API` -> webApi
  - else `TWITTER_CONSUMER_KEY` + `TWITTER_CONSUMER_SECRET` -> developerApi
  - `TWITTER_ACCESS_TOKEN` / `TWITTER_ACCESS_SECRET` exist in config.ts but are not what gates init
- Handle validity could NOT be verified: x.com returns 200 even for a nonsense handle
  (JS shell / login wall), so status-code probing is useless. Re-verify handles after auth works.
- User added `TWITTER_AUTH_TOKEN` to `/Users/ivanwong/Code/business/rsshub/.env` (gitignored).
  After restart all routes returned 200. **Final state: 32/32 feeds healthy.**

## Handle corrections applied (all via `edit_feed`)
- `mustafa_suleyman` -> `mustafasuleyman` (old handle 503 "incomplete user response")
- `clem_delangue` -> `ClementDelangue`
- `xai` -> `grok` (renamed "Grok by xAI"); @xai resolves but always returns 0 items
- `BenedictEvans` -> `DrJimFan` (Jim Fan). @BenedictEvans resolves to the right person but
  consistently 0 items — he seems inactive on X. His blog is already subscribed in folder 07.
- Failure signatures worth knowing: wrong handle => 503 "incomplete user response";
  valid-but-inactive account => 200 with 0 `<item>`.

## Other
- `get_feed_tree` can transiently return `[]` (app mid-reload). Retry a few times before
  concluding data is gone — it came back with all 16 top-level folders intact.
